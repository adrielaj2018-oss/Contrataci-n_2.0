PORTAL HR PRO - SOLO GESTIÓN DE CONTRATACIÓN

Contenido listo para subir a GitHub/Render.

IMPORTANTE:
1. Sube el CONTENIDO de esta carpeta a la raíz del repositorio, no la carpeta completa.
2. Render debe encontrar en la raíz: app.py, requirements.txt, Procfile, runtime.txt.
3. Build Command: pip install -r requirements.txt
4. Start Command: gunicorn app:app --bind 0.0.0.0:$PORT --workers 1 --threads 8 --timeout 120
5. Usuario demo: admin / admin123

Se dejó activo únicamente el módulo Gestión de Contratación y se bloquearon accesos a Gestión Documental/Vacacional.
